import { createTheme } from "@mantine/core";

export const COLOURS = {
	primary: "#3895a0",
	secondary: "#111f30",
	black: "#09101a",
};

export const theme = createTheme({
	primaryColor: "cyan",
	black: COLOURS.black,
});
